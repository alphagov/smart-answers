<% text_for :title do %>
  You need a visa to work in the UK
<% end %>

<% govspeak_for :body do %>
  Based on your answers, you have the following visa options.

  <%= render partial: 'stateless_or_refugee', locals: {calculator: calculator} %>

  ----

  <%= render partial: 'visa_types/country_in_bno_list', locals: { calculator: calculator } %>

  <%= render partial: 'visa_types/eligible_skilled_job_other', locals: { calculator: calculator } %>

  ----

  <%= render partial: 'visa_types/work_at_multinational_company' %>

  ----

  <%= render partial: 'visa_types/country_in_youth_mobility_scheme', locals: { calculator: calculator } %>

  <%= render partial: 'visa_types/uk_ancestry_visa', locals: { calculator: calculator } %>

  ----

  <%= render partial: 'visa_types/temporary_worker_charity', locals: { calculator: calculator } %>

  ----

  <%= render partial: 'visa_types/temporary_worker_government_organised_exchange', locals: { calculator: calculator } %>

  ----

  <%= render partial: 'visa_types/temporary_worker_international_agreement_service', locals: { calculator: calculator } %>

  ----

  <%= render partial: 'visa_types/temporary_worker_international_agreement_work', locals: { calculator: calculator } %>
<% end %>
