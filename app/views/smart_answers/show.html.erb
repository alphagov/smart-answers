<% content_for :title do %><%= @presenter.title %><% end %>
<main id="content" role="main" <% if @presenter.started? %> class="smart-answer-questions group"<% end %>>
  <%= render partial: "content" %>
</main>
<div class="meta-wrapper<% if @presenter.started? %> started<% end %>">
  <div id="report-a-problem"></div>

  <%= render :partial => 'smartanswer_metadata' %>
</div>
