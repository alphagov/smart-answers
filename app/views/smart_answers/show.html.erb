<% content_for :title do %><%= @presenter.title %><% end %>
<% content_for :section_meta_tags do %>
<meta name="x-section-name" content="<%= @presenter.section_name %>" />
<meta name="x-section-link" content="<%= @presenter.section_slug %>" />
<% end %>
<section id="content"<% if @presenter.started? %> class="smart-answer-questions"<% end %>>
  <%= render partial: "content" %>

  <div class="meta-data">
    <div class="inner">
      <div class="print-and-modified-date group">
        <p class="modified-date">Last updated: <%= last_updated_date.strftime('%d %B %Y') %></p>
      </div>
    </div>
  </div>
</section>
