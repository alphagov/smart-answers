<div class="article-container outcome">
  <article class="outcome group">
    <div class="result-info">
      <div class="inner group">
        <% if outcome.has_title? %>
        <div class="summary"><h2 class="result-title"><%= outcome.title %></h2></div>
        <% end %>
        <% if outcome.has_body? %>
          <%= outcome.body %>
        <% end %>
      </div>
    </div>
    <% if outcome.has_next_steps? %>
      <div class="related-wrapper">
        <div class="related next-steps">
          <h2>Next steps</h2>
          <%= outcome.next_steps %>
        </div>
      </div>
    <% end %>
  </article>
</div>

<script type="text/javascript">
  $(function() {
    var flowName = '<%= @name %>';
    var page = document.location.pathname;
    var options = {
      label: flowName,
      nonInteraction: true,
      page: page
    };
    GOVUK.analytics.trackEvent('Smart Answer', 'Completed', options);
  });
</script>
