<article class="results">
  <div class="inner">
    <h2><%= @presenter.current_node.title %></h2>
    <% if @presenter.current_node.has_body? %>
      <%= @presenter.current_node.body %>
    <% end %>
  </div>
</article>