<!DOCTYPE html>
<html>
  <head>
    <title><%= yield :title %> | www.gov.uk</title>
    <%= stylesheet_link_tag "smart-answers" %>
    <%= javascript_include_tag "smart-answers", "holiday-pay", defer: true %>
    <meta name="x-section-name" content="<%= yield(:section_name).gsub('-', ' ').capitalize %>" />
    <meta name="x-section-link" content="/browse/<%= yield :section_name %>" />
    <%= yield :head %>
  </head>
<body class="citizen">
  <div id="wrapper" class="answer smart_answer">
    <%= yield %>
  </div>
</body>
</html>
