
engines/
this contains the integration tests for the smart answers engine itself.  This hits the full stack
using some sample flows (stored in test/fixtures/flows), and tests both Javascript and non-Javascript behaviour.
